# Deferred Notes

## Rendering / Asset Format

- 2026-07-12: Venue recursive dependency diagnostics now split MILO entries into visual scene objects, venue animation routes, camera-phase objects, and sound-bank objects instead of only `visual`/`bank`. This keeps camera objects out of venue-load failure triage while preserving animation coverage proof. All-venue deterministic sweep `engine/out/venue_dependency_bucket_sweep_20260712_001` exits 0 for battle/arena/theatre/fest/big/small1/small2/stone with no `decode failed`, `unsupported`, or `unresolved` rows in the summary; focused raw RedOctane probe `engine/out/venue_dependency_bucket_big_probe_20260712_001/run.log` logs `world/big/gen/big_bank.milo_ps2 dir=ObjectDir entries=58 visual=0 anim=0 camera=0 bank=58` and no bad rows.
- 2026-07-12: Venue/MILO `RndParticleSys` decode now follows ihatecompvir/rb3 class order for GH2 rev27 (`Hmx::Object`, `RndAnimatable`, `RndTransformable`, `RndDrawable`, then particle fields) instead of the old traced `Trans` byte offset. RedOctane probe `engine/out/redoctane_particlesys_source_order_probe_20260712_002` logs `world/big/og/gen/big_geom.milo_ps2` as `18 particles (18 ok / 0 fail, 18 source-order)` and both flashpot proxy loads as `6 particles (6 ok / 0 fail, 6 source-order)`, with every logged `ParticleSys` stamped `source_order=1 rev=27 anim_rev=4 trans_rev=9 draw_rev=3`.
- 2026-07-12: Small1 venue `web_lights_glows.part` proves GH2 rev27 preserved-particle payloads can use 8-float rows (`XYZ`, `RGBA`, `size`) even though later rb3/MiloEditor preserved particle rows read as 9 floats. Probe `engine/out/small1_particlesys_preserved_stride_probe_20260712_001` logs `web_lights_glows.part source_order=1 preserve=1 preserved=169 preserved_stride=32` and `world/small1/og/gen/small1_geom.milo_ps2` as `10 particles (10 ok / 0 fail, 10 source-order)`. Sweep `engine/out/venue_load_sweep_20260712_002` exits 0 for all eight venues and reports no venue particle decode failures.
- 2026-07-12: Venue `WorldCrowd` loading now treats ihatecompvir `WorldCrowd::mNum` as the source field it is, not as an exact sum of decoded per-character placement rows. Stone probe `engine/out/stone_worldcrowd_mnum_probe_20260712_001` logs all six Stone chars WorldCrowd records as `6 world_crowd (6 ok / 0 fail)`, records `mNum` and decoded row counts for `crowd_left`/`crowd_right`/`crowd_encore`/`crowd_center`/`crowd`/`crowd_hill`, and reaches runtime with `WorldCrowd runtime ready: actors=3 placements=1761 basis=placement`.
- 2026-07-12: Venue/MILO `Spotlight` decode now follows ihatecompvir `Spotlight::Load` source order for GH2 rev20 (`Hmx::Object`, `RndDrawable`, `RndTransformable`, `spot_scale`/`spot_height`, old counted `BeamDef` vector, light-can group, target, color/intensity, disc/flare/lens fields, additional objects, target-shadow, animate-from-preset). RedOctane probe `engine/out/redoctane_spotlight_source_order_probe_20260712_004` logs all 33 lighting Spotlights as `source_order=1`, including the two STROBE rows whose source `BeamDef` vectors contain 3 beams; Stone probe `engine/out/stone_spotlight_source_order_probe_20260712_001` logs all 64 lighting Spotlights as `source_order=1`, with no Spotlight fallback/error rows.
- 2026-07-12: Venue/MILO `RndEnviron` decode now follows ihatecompvir source order for GH2 rev5 (`Hmx::Object`, old light list, ambient color, fog start/end/color, fog enable, animate-from-preset, fade-out/start/end) instead of partially source-shaped byte offsets. Scene diagnostics now log geometry and lighting Environs with `source_order`/revision/fade proof. RedOctane probe `engine/out/redoctane_environ_source_order_probe_20260712_001` logs `stage.env`, `gears_light.env`, `fan_light.env`, `curtain_light`, band/crowd/character Environs, and track Environs as `source_order=1 rev=5`; Stone probe `engine/out/stone_environ_source_order_probe_20260712_001` logs 25 source-order Environ rows including `stone_geom`, `stone_lighting`, chars, and track Environs, with no Environ decode failure or fallback rows.
- 2026-07-12: Venue `EventTrigger` animation/visibility routing now prefers ihatecompvir source order instead of the old rev8-only/scanner split: Hmx object fields, trigger events, Anim rows, sounds/shows/draws or hide delays, enable/disable/wait, next links, and proxy calls are decoded into one source-shaped record. RedOctane probe `engine/out/redoctane_eventtrigger_source_order_probe_20260712_001` logs 29 `venue EventTrigger ... source_order=1 rev=8` rows and active `fan_gears_anims_*` launches; Stone probe `engine/out/stone_eventtrigger_source_order_probe_20260712_001` logs 76 source-order EventTriggers including visibility show/draw routes, with no EventTrigger source-order fallback/failure rows.
- 2026-07-12: Venue `LightPreset` decode now follows ihatecompvir source order for GH2 rev8 (`Hmx::Object`, `RndAnimatable`, counted keyframes, counted spot/env/light refs, looping/category/select-trigger/fade/manual/locked fields) instead of the old byte/label scanner. RedOctane probe `engine/out/redoctane_lightpreset_source_order_probe_20260712_001` logs all 20 presets as `source_order=1 rev=8 anim_rev=4`; Stone probe `engine/out/stone_lightpreset_source_order_probe_20260712_001` logs all 34 presets source-order plus 20 source-order lighting `Set` rows, with no LightPreset source decode failures.
- 2026-07-12: Venue lighting `RndSet` decode now follows ihatecompvir source order for GH2 rev0 (`Hmx::Object` then counted object symbols) instead of fixed offsets. Stone probe `engine/out/stone_set_source_order_probe_20260712_001` logs 20 `lighting Set <name> source_order=1 rev=0` rows before LightPreset Set refs expand into spotlight refs; RedOctane has no lighting `Set` entries in the traced world inventory.
- 2026-07-12: Venue/MILO `RndLight` decode now follows ihatecompvir source order for GH2 rev6 (`Hmx::Object`, `RndTransformable`, color, range, pre-rev14 adjusted type, animate-color/position flags) instead of raw offset reads. Stone probe `engine/out/stone_light_source_order_probe_20260712_001` logs `fire_bonfiire_stack.lit source_order=1` with `anim_range=1` and exits normally while lighting MatAnim samples continue firing.
- 2026-07-12: Venue/MILO `RndGroup` decode now prefers ihatecompvir source class order (`Hmx::Object`, `RndAnimatable`, GH2 `RndTransformable`, `RndDrawable`, then group object/env/draw-only/lod/sort fields) before the older transform scanner fallback. RedOctane probe `engine/out/redoctane_group_source_order_probe_20260712_001` loads `big_geom` with `23 group (23 source-order)` and exits normally while venue AnimFilter samples continue firing.
- 2026-07-12: Venue/RndDir `RndMeshAnim` decode now follows ihatecompvir/rb3 source order instead of fixed byte offset 25: optional `Hmx::Object`, `RndAnimatable`, mesh target, position/normal/UV/color key pages, and required `keysOwner`. RedOctane probe `engine/out/redoctane_meshanim_source_reader_probe_20260712_002` shows no `RndMeshAnim` decode failures and decodes `flashpot_flash.msnm` UV keys.
- 2026-07-12: Battle lighting `electric_outlet_anim.msnm` proves GH2 `RndMeshAnim` key frames can start before frame 0. The native MeshAnim reader now accepts finite negative source key frames instead of rejecting them as invalid; probe `engine/out/battle_meshanim_negative_frame_probe_20260712_001` decodes `electric_outlet_anim.msnm -> electric_outlet_anim.mesh` with `pos_keys=21`, `verts=342`, and `duration=419.0`, and sweep `engine/out/venue_meshanim_negative_frame_sweep_20260712_001` exits 0 for all eight venues with zero MeshAnim decode failures.
- 2026-07-12: Venue `RndAnimFilter::Scale` now preserves authored negative `scale` values instead of taking `fabs(scale)`, matching ihatecompvir `RndAnimFilter::Load`/`Scale`. Current sweep `engine/out/venue_gap_sweep_20260712_002/fest/run.log` proves GH2 authors `motocross01.filt` with `scale=-1.000`; keeping the sign is required for correct native animation direction.
- 2026-07-12: Venue/proxy `RndParticleSysAnim` decode now follows ihatecompvir/rb3 source order instead of fixed byte offset 25: optional `Hmx::Object`, `RndAnimatable`, particle system, start/end color keys, emit-rate keys plus `keysOwner`, then speed/life/start-size keys. Probe folders `engine/out/redoctane_particlesys_source_reader_probe_20260712_001` and `engine/out/venue_particlesys_source_probe_arena_20260712_001` show no `RndParticleSysAnim` decode failures; `arena` loads keyed flame/spark emit-rate and speed channels.
- 2026-07-12: Venue `RndEnvAnim`/`RndLightAnim` decode now follows ihatecompvir/rb3 source order instead of fixed byte offset 25: optional `Hmx::Object`, `RndAnimatable`, target object, keyed color/fog tracks, and `keysOwner`. Probe folders `engine/out/redoctane_env_light_source_reader_probe_20260712_001`, `engine/out/venue_env_light_load_probe_arena_20260712_001`, and `engine/out/venue_env_light_load_probe_stone_20260712_001` show no `RndEnvAnim`/`RndLightAnim` decode failures; `arena` loads keyed cop-light EnvAnims and `stone` loads keyed fire LightAnims plus UFO EnvAnims.
- 2026-07-12: Venue `RndMatAnim` rows with no local key channels are only treated as true no-ops when their source `keysOwner` is empty/self. Owner-backed rows now stay loadable/routable long enough to inherit keyed material channels, matching ihatecompvir's `RndMatAnim` read order. RedOctane probe `engine/out/redoctane_matanim_owner_probe_20260712_001` confirms `fan_floorspot.mnm` and `smoke_lights.mnm` sample live texture transform channels.
- 2026-07-12: Venue `RndMatAnim` decode now uses the ihatecompvir/MiloEditor order (`ObjectFields`, `RndAnimatable`, material, pre-rev7 stages when present, `keysOwner`, color/alpha/texture transform keys) instead of the old fixed byte offset. RedOctane probe `engine/out/redoctane_matanim_source_reader_probe_20260712_001` has no `RndMatAnim decode failed` rows and confirms live `fan_floorspot.mnm`/`smoke_lights.mnm` texture transform sampling. MSVC `ghogx_game` now uses `/bigobj` because the source-shaped venue reader work pushed the large `gameplay.cpp` object over the default section limit.
- 2026-07-12: Small2 empty-target parent `RndTransAnim` rows such as `speaker1.tnm` and `amp4.tnm` stay routable by anim object name for source timing, but do not become render targets because rb3 `RndTransAnim::SetFrame` no-ops when `mTrans` is null. Forced `--diagnostic-venue-event @filter:speaker1_hit_p0_fret2` now logs `source no-op` rows with zero `mesh=speaker1.tnm` render samples; normal autoplay primarily exercises the direct `speaker_cone*.tnm` hit filters.
- 2026-07-12: RedOctane venue gears are still not complete. Several gears have wrong-facing/local-axis behavior and need active user visual review against original gameplay before marking the venue animation pass done.
- 2026-07-12: RedOctane gear diagnostics now log sampled local delta axes in addition to raw absolute quaternion axes. Probe `engine/out/redoctane_gear_delta_axis_probe_20260712_001` shows all sampled `gear_opt*` rows at source frame 1.0 with `recompose_delta_axis_dom=z` and `local_delta_axis_dom=z`, matching each gear mesh `face_axis=z`; CW rows carry negative Z deltas while CCW/small CCW rows carry positive Z deltas. This narrows the remaining gear review to facing/target/original-game comparison instead of the basic source-local rotation axis.
- 2026-07-12: RedOctane gear review should use `GHOGX_LOG_MESH_ANIM_LOCAL=gear_opt` / `GHOGX_LOG_MESH_ANIM_WORLD=gear_opt`; the diagnostics now report the authored animated target kind and parent for each sampled gear mesh.
- 2026-07-12: RedOctane gear axis probe is at `engine/out/redoctane_gear_axis_probe`. Sampled `gear_opt_ccw*` axes are authored local Z, `gear_opt_cw*` axes are authored local X/Y-plane quats, and `gear_opt_small_ccw*` axes are authored local Y-dominant. Compare these with original gameplay before changing transform math.
- 2026-07-12: RedOctane audience floor is verified present/renderable in freecam. Pink surfaces in the venue inspector are active crosshair highlights, not missing-material or bad-floor evidence.
- 2026-07-12: RedOctane fan animation is verified moving through the authored shuttle cycle. Use `GHOGX_DEBUG_VENUE_FILTER_STRIDE` and `GHOGX_LOG_MESH_ANIM_STRIDE` with low values when checking fast venue animations so half-second/30-render diagnostics do not alias the motion as stationary.
- 2026-06-23: Investigate apparent UV mapping issues visible in native venue screenshots. The user-provided example at `C:\Users\smmel\AppData\Local\Temp\codex-clipboard-58e40d45-095e-4972-bd59-5e7577f4ca9a.png` shows stretched/repeated striped textures across venue surfaces and props. Treat this as a later shared texture coordinate/material decode audit, not a venue-specific visual hack. Do not pull it into the current character hand/venue lighting priority unless a traced venue-format fix points directly at it.
